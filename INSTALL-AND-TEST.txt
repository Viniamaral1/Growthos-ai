GrowthOS — Context popover and header tooltip correction

Replace only these files in the project:

frontend/app/components/CofounderChat.tsx
frontend/app/globals.css

Then, from the project root:

Remove-Item -Recurse -Force .\frontend\.next -ErrorAction SilentlyContinue
cd frontend
npm run dev

Hard-refresh the browser with Ctrl + Shift + R.

Test only:
1. Hover Context: a navy tooltip appears.
2. Open Context, then click outside: it closes.
3. Open Context, then press Esc: it closes.
4. Open Context, then open Team or conversation history: Context closes.
5. Change the Context scope: the menu closes after selection.
6. Hover the top header controls: their navy tooltips open below the buttons and are not clipped by the top edge.

No chat scrolling, composer layout, backend, routing, Knowledge, Dashboard, Settings, or other page logic was changed.
